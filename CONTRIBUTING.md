# 贡献指南

感谢你愿意为这个项目贡献代码！请遵循以下几点：

## 提 Issue

- 描述清楚问题：运行环境（系统 / Python / Pygame 版本）、复现步骤、期望与实际行为
- 附上完整的报错信息或截图

## 提 PR

1. Fork 本仓库并新建分支：`git checkout -b feature/xxx`
2. 保持代码风格与现有文件一致（Python 3、PEP 8 基本规范、注释为中文）
3. 改动前先运行 `python main.py` 确认游戏可正常启动
4. 提交信息简洁明了，描述改动内容与原因
5. 如有必要，同步更新 README

## 开发约定

- 不提交 `record.txt`（已在 `.gitignore` 中忽略）
- 新增资源（图片 / 音效）请放入 `images/` 或 `sound/` 对应目录
